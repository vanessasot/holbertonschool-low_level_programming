#include "holberton.h"

/**
 * mul - Multiplies two integers.
 * @a: Number to multiplies.
 * @b: Number to multiplies.
 * Return: 0 if successful.
 */

int mul(int a, int b)
{
	int op;

	op = a * b;
	return (op);
}
